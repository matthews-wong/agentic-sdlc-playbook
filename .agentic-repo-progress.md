# Build Progress — agentic-sdlc-playbook

This repo is built iteratively by a `/loop` (every 10 min). Each iteration deepens or adds a section, commits, and pushes. This file tells the next iteration what exists and what to do next, so a fresh session can continue coherently.

## Repo intent
A source-backed knowledge base on **agentic SDLC** and **agentic workflows**. Documentation repo (Markdown), not a code project.

## Done
### Iteration 1 (2026-08-16)
- Scaffolded repo: README, LICENSE (MIT), .gitignore.
- `docs/agentic-sdlc.md` — lifecycle stage-by-stage, roles, human checkpoints, the "6+ stages" threshold, adoption sequence.
- `docs/agentic-workflows.md` — cognitive patterns (reflection/tool-use/planning/ReAct), the five orchestration workflows, multi-agent orchestration, when NOT to build an agent, worked example.
- `docs/governance-and-metrics.md` — guardrails by blast radius, HITL design, observability, metrics that prove value.
- `docs/references.md` — all sources.
- Research: 3 web searches (agentic SDLC 2026, agentic workflow patterns, Anthropic Building Effective Agents).

### Iteration 2 (2026-08-16)
- Added `docs/patterns/` catalog: index + one file per orchestration workflow (prompt-chaining, routing, parallelization, orchestrator-workers, evaluator-optimizer), each with a framework-agnostic code sketch and trade-offs.
- Linked the catalog from `docs/agentic-workflows.md`.
- Push still blocked (no PAT present at fire time) — content committed locally.

### Iteration 3 (2026-08-16)
- Added `docs/case-studies.md`: GitHub Copilot coding agent (real, sourced), an end-to-end Azure+GitHub reference architecture, and the "narrow-adoption" anti-case — each mapped to the patterns and governance sections.
- Linked case studies + pattern catalog from README contents; added case-study sources to references.md.
- Push still blocked (no PAT at fire time) — committed locally.

### Iteration 4 (2026-08-16)
- Added `docs/tooling-landscape.md`: agent-building frameworks (Claude Agent SDK, LangGraph, CrewAI, OpenAI Agents SDK, AG2, Strands) vs. SDLC agent products, mapped to patterns, plus 8 selection criteria. Grounded in a web search of 2026 framework comparisons.
- Wired into README + references. Push still blocked (no PAT at fire time) — committed locally.

### Iteration 5 (2026-08-16)
- Added `docs/anti-patterns.md`: a 10-entry catalog of failure modes (autonomy theater, narrow adoption, assertion-as-done, rubber-stamp review, self-review blindness, flying blind, unbounded loops, blast-radius blindness, framework-first, vanity metrics) — each with smell/cause/fix and cross-links.
- Wired into README + cross-linked from agentic-workflows.md. Push still blocked (no PAT at fire time) — committed locally.

### Iteration 6 (2026-08-16)
- Added `docs/diagrams.md`: Mermaid diagrams for the agentic SDLC loop, all five orchestration workflows, and the guardrail-by-blast-radius decision tree. Embedded the SDLC loop diagram into agentic-sdlc.md.
- Wired into README. Push still blocked (no PAT at fire time) — committed locally.

### Iteration 7 (2026-08-16)
- Added `CONTRIBUTING.md`, `.github/ISSUE_TEMPLATE/` (content_suggestion, correction, config), `.github/PULL_REQUEST_TEMPLATE.md`.
- Added `.github/workflows/docs-lint.yml` (markdownlint-cli2 + lychee link check) and a lenient `.markdownlint.json`. Added CI badge + Contributing note to README.
- **Backlog now empty — core playbook is feature-complete.** Future iterations = depth passes (see "Deepening ideas" below).
- Push still blocked (no PAT at fire time) — committed locally.

### Iteration 8 (2026-08-16)
- Added `examples/release-notes-agent/`: a runnable, framework-free agent (prompt chaining + gate + evaluator-optimizer) with a deterministic mock backend, a real-Claude backend, a README, and 3 hermetic tests. **Verified: `python agent.py` runs and `pytest` → 3 passed.**
- Wired into README.
- Push still blocked (no PAT at fire time) — committed locally.

### Iteration 9 (2026-08-16)
- Added `docs/glossary.md` (20+ linked terms) and `docs/faq.md` (9 Q&As), both cross-linked into the existing docs. Wired into README.
- Extended `.github/workflows/docs-lint.yml` with an `example-tests` job (setup-python + pytest on the release-notes agent).
- Push still blocked (no PAT at fire time) — committed locally.

### Iteration 10 (2026-08-16)
- Added `docs/patterns/cognitive-patterns.md`: reflection, tool use, planning, ReAct — each with a code sketch, plus a compose-with-orchestration table. Updated patterns/README to link it.
- Push still blocked (no PAT at fire time) — committed locally.

### Iteration 11 (2026-08-16)
- Added `examples/ticket-triage-agent/`: a routing + parallelization agent (classify → dispatch; bug fans out into concurrent severity/component checks) with mock backend, README, and 4 hermetic tests. **Verified: demo runs, pytest → 4 passed.**
- CI `example-tests` job now loops over all `examples/*/` dirs. Wired into README.
- Push still blocked (no PAT at fire time) — committed locally.

## Backlog
_Two runnable examples now (iterations 8, 11)._ Remaining deepening ideas:
- A "further reading" curated list / `docs/further-reading.md`.
- A third example or a hands-on tutorial stitching stages into an end-to-end mini SDLC.

### Iteration 12 (2026-08-16) — 🎉 PUSHED
- **Token received & validated** (login matthews-wong; scopes include repo + workflow). Created the GitHub repo via REST API (HTTP 201), pushed all commits. Local HEAD == origin/main.
- Scrubbed the token out of `.git/config` (the `-u tokenized-URL` had embedded it), pointed branch.main at clean `origin`, and stored the PAT in Windows Credential Manager so future pushes are silent.
- Added `docs/further-reading.md` (curated path with why-to-read notes); wired into README. Pushed via `origin` (silent, GCM-backed).
- **Repo is now LIVE: https://github.com/matthews-wong/agentic-sdlc-playbook**

### Iteration 13 (2026-08-16)
- Added `docs/adoption-roadmap.md`: a 30/60/90-day rollout plan tying stages + governance + metrics into an actionable sequence with exit criteria. Wired into README. Pushed via origin (silent, token stored).

### Iteration 14 (2026-08-17)
- Added `docs/security.md`: agentic threat model (dynamic surface, multi-source trust, high blast radius), prompt injection (direct/indirect ≈ RCE with tools), the defense stack (least privilege, sandboxing, tool-use governance), and a security checklist. Grounded in a web search of 2026 agentic-security sources. Wired into README + references. Pushed via origin (silent).

### Iteration 15 (2026-08-17)
- Added `docs/evaluating-agents.md`: three eval layers (final-answer/trajectory/per-turn), LLM-as-judge limits (cost + variance → offline only), regression testing under non-determinism (sample; build from real failures; gate deterministic scaffold), benchmarks vs. custom datasets. Grounded in a 2026 agent-eval web search. Wired into README + references. Pushed via origin (silent).

### Iteration 16 (2026-08-17)
- QA: audited all relative Markdown links across 31 files — clean (the only 2 hits were false positives from `tools[name](arg)` inside fenced code blocks, which link-checkers ignore).
- Added `CHANGELOG.md` (Keep a Changelog format) documenting the build history. Pushed via origin (silent).

### Iteration 17 (2026-08-17)
- Added `docs/cost-and-performance.md`: cost-per-completed-task mindset, the stacked levers (prompt caching with the cache-first ordering rule, model routing/tiering, compression, batching, self-hosted), latency levers, and testing orchestration against a mock at zero cost. Grounded in a 2026 cost-optimization web search. Wired into README + references. Pushed via origin (silent).

### Iteration 18 (2026-08-17)
- Added `docs/protocols-mcp-a2a.md`: the two-layer interoperability stack (MCP vertical/tools, A2A horizontal/agent-coordination), ACP + Linux Foundation context, and governance/security caveats (protocols standardize connectivity, not safety). Grounded in a 2026 protocols web search. Wired into README + references. Pushed via origin (silent).

### Iteration 19 (2026-08-17)
- Added `docs/context-and-memory.md`: context rot (silent degradation as tokens grow), short/long-term memory + compaction, governance decay (compaction erasing safety constraints) with mitigations, and just-in-time retrieval. Grounded in a 2026 context-engineering web search. Wired into README + references. Pushed via origin (silent).

### Iteration 20 (2026-08-17)
- Regression check: both example suites green (release-notes 3 passed, ticket-triage 4 passed).
- Added `docs/README.md` — a grouped, journey-ordered index of all 13 guides + examples (Foundations / Design / Build / Operate / Advanced / Reference). Linked from the main README Contents. Pushed via origin (silent).

### Iteration 21 (2026-08-17)
- Added `docs/reliability-and-recovery.md`: transient-vs-terminal error classification, retry strategy (backoff+jitter, budgets, circuit breaker), idempotency keys, durable state + saga/compensation, checkpointing, and human-escalation triggers. Grounded in a 2026 fault-tolerance web search. Wired into README + references. Pushed via origin (silent).

### Iteration 22 (2026-08-17)
- Added `docs/team-and-operating-model.md`: delegate/review/own model, creator→curator role shift, new roles (agentic architect, knowledge architect, agent reliability engineer), and org patterns (centralized platform team à la LinkedIn; lifecycle tiger teams à la Red Hat). Grounded in a 2026 operating-model web search. Wired into README + references. Pushed via origin (silent).

### Iteration 23 (2026-08-17)
- Added `docs/observability.md`: OTel GenAI semantic conventions (experimental status caveat), the span/trace model mirroring the reasoning chain, token-based cost attribution, the three signals, and the covers/stops boundary vs evaluation. Grounded in a 2026 OTel-GenAI web search. Wired into README + references. Pushed via origin (silent).

### Iteration 24 (2026-08-17)
- Added `docs/prompting-and-instructions.md`: system prompt as runbook (goal/steps/tool-policy/stop/constraints), reasoning-before-action, structured output, tool-description design (iterate from traces), context-region segmentation, and explicit multi-agent handoffs. Grounded in a 2026 prompting web search (incl. Anthropic tools-for-agents). Wired into README + references. Pushed via origin (silent).

### Iteration 25 (2026-08-17)
- Added `docs/compliance-and-regulation.md`: EU AI Act (high-risk obligations now in force as of Aug 2 2026), NIST AI RMF, ISO/IEC 42001, the agentic gap (frameworks not designed for agents; Singapore + NIST CAISI initiative), a pragmatic RMF→42001→EU-Act sequence, and a checklist. Not-legal-advice disclaimer. Grounded in a 2026 AI-governance web search. Wired into README + references. Pushed via origin (silent).

### Iteration 26 (2026-08-17)
- QA + tooling: added `scripts/check_links.py` — a stdlib, anchor-aware internal-link checker (strips fenced code entirely and inline code for link detection, but keeps inline-code words in heading slugs à la GitHub). Wired it into CI as an `anchor-links` job; documented in CONTRIBUTING.
- Found & fixed a real bug in the checker itself (inline code in headings dropped from slugs); final run is clean — all internal file + #anchor links across 41 files resolve. Pushed via origin (silent).

### Iteration 27 (2026-08-17)
- Added `docs/rag-and-grounding.md`: grounding vs hallucination, "failure moves to the retrieval layer", the four levers (semantic chunking, hybrid retrieval, reranking, long-context tradeoff), per-claim citations, agentic multi-hop RAG, and the untrusted-content caveat. Grounded in a 2026 RAG web search. Wired into README + references. Verified with check_links.py. Pushed via origin (silent).

### Iteration 28 (2026-08-17)
- Added `docs/agentic-coding.md`: spec-driven development (spec as source of truth), spec structure (outcomes/scope/constraints/decisions/tasks/verification), the spec→plan→tasks→code flow with review gates, small-task shipping, tests-as-spec, and review discipline. Grounded in a 2026 SDD web search. Wired into README + references. Verified with check_links.py. Pushed via origin (silent).

### Iteration 29 (2026-08-17)
- Added `docs/maturity-model.md`: a five-level model (Manual → AI-assisted → Agent+review → Orchestrated → Autonomous), "L3 is the 2026 frontier" guidance, per-level "what to build next", and an eight-dimension self-assessment table linking to each guide (a synthesizing capstone). Grounded in a 2026 maturity-model web search. Wired into README + references. Verified with check_links.py. Pushed via origin (silent).

### ✅ Completed backlog
1. ~~`docs/case-studies.md`~~ (iteration 3)
2. ~~`docs/tooling-landscape.md`~~ (iteration 4)
3. ~~`docs/anti-patterns.md`~~ (iteration 5)
4. ~~Mermaid diagrams~~ (iteration 6)
5. ~~`CONTRIBUTING.md` + issue templates~~ (iteration 7)
6. ~~GitHub Actions doc-lint~~ (iteration 7)

## ⚠️ Push / token status
- **BLOCKER (iteration 1):** No GitHub PAT is present in Windows Credential Manager (`cmdkey /list` shows no `github.com` entry; git config still expects `githubauthmodes=pat`). `git credential fill` blocks on the interactive GCM prompt.
- Local git repo is initialized and committed. Remote `origin` is set to `https://github.com/matthews-wong/agentic-sdlc-playbook.git` but the repo does **not** exist on GitHub yet and cannot be created without a token.
- **To unblock:** provide a fresh PAT with `repo` scope **AND `workflow` scope** (as of iteration 7 the repo contains `.github/workflows/docs-lint.yml`; classic PATs need the separate `workflow` scope, or fine-grained PATs need Workflows: read/write, to push workflow files — otherwise the push is rejected). Once present, an iteration will (a) create the GitHub repo via the REST API and (b) `git push -u origin main`.
- Do NOT trigger the blocking GCM popup again in a non-interactive loop.
