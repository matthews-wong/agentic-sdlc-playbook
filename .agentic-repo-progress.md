# Build Progress — agentic-sdlc-playbook

This repo is built iteratively by a `/loop` (every 10 min). Each iteration deepens or adds a section, commits, and pushes. This file tells the next iteration what exists and what to do next, so a fresh session can continue coherently.

## Repo intent
A source-backed knowledge base on **agentic SDLC** and **agentic workflows**. Documentation repo (Markdown), not a code project.

## Done
### Iteration 1 (2026-08-16)
- Scaffolded repo: README, LICENSE (MIT), .gitignore.
- `docs/agentic-sdlc.md` — lifecycle stage-by-stage, roles, human checkpoints, the "6+ stages" threshold, adoption sequence.
- `docs/agentic-workflows.md` — cognitive patterns (reflection/tool-use/planning/ReAct), the five orchestration workflows, multi-agent orchestration, when NOT to build an agent, worked example.
- `docs/governance-and-metrics.md` — guardrails by blast radius, HITL design, observability, metrics that prove value.
- `docs/references.md` — all sources.
- Research: 3 web searches (agentic SDLC 2026, agentic workflow patterns, Anthropic Building Effective Agents).

## Backlog (pick the top item next iteration)
1. `docs/patterns/` — one file per pattern with a minimal code sketch (prompt chaining, routing, parallelization, orchestrator-workers, evaluator-optimizer).
2. `docs/case-studies.md` — concrete write-ups (e.g. Microsoft/GitHub end-to-end agentic SDLC) distilled into what worked / what to copy.
3. `docs/tooling-landscape.md` — survey of agent frameworks & SDLC agent products, with selection criteria.
4. `docs/anti-patterns.md` — expand the "when not to build an agent" section into a standalone catalog of failure modes.
5. A diagram (Mermaid) of the agentic SDLC loop and of each orchestration workflow, embedded in the docs.
6. `CONTRIBUTING.md` + issue templates.
7. Add a GitHub Actions doc-lint (markdownlint + lychee link check).

## ⚠️ Push / token status
- **BLOCKER (iteration 1):** No GitHub PAT is present in Windows Credential Manager (`cmdkey /list` shows no `github.com` entry; git config still expects `githubauthmodes=pat`). `git credential fill` blocks on the interactive GCM prompt.
- Local git repo is initialized and committed. Remote `origin` is set to `https://github.com/matthews-wong/agentic-sdlc-playbook.git` but the repo does **not** exist on GitHub yet and cannot be created without a token.
- **To unblock:** provide a fresh PAT with `repo` scope. Once present, an iteration will (a) create the GitHub repo via the REST API and (b) `git push -u origin main`. See repo-create + push helper in the loop notes.
- Do NOT trigger the blocking GCM popup again in a non-interactive loop.
